{
  "name": "fullstackopen",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd /Users/ijane/Documents/coding/reactintro/introdemo && npm run build && cp -r dist /Users/ijane/Documents/coding/fullstackopen",
    "deploy:full": "npm run build:ui && git add . && git commit -m iubuild && git push"
  },
  "author": "Eri Barrett",
  "license": "EARTH",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
